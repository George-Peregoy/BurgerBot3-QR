"""
Standalone, ROS-independent EBS printer client: project generation
(prj_gen), preview rendering (prv_gen), .exp packaging (exp_build), and
the HTTP client (http_client) that talks to the printer.

`python -m ebs_printer.cli "text"` runs the whole pipeline end to end
as a debug harness -- see cli.py.
"""
